<div class="makeReplyBlock">
    <div class="postCommentUserIconReply" style="background-image: url({{ asset(app.user.avatarUrl) }}); background-size: cover;"></div>
    <div class="mkrRight">
        <input placeholder="{{ 'Напишите свой комментарий'|trans }}" type="text">
        <button
            class="publishButtonComment"
            data-create-replay-url="{{ createReplayUrl }}"
            data-_csrf_token="{{ csrf_token('authenticate') }}"
        >{{ 'Опубликовать'|trans }}</button>
    </div>
</div>